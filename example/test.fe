def func(x, y...) =
 IO_println(x, y);
;

class X =
 def init(x...) =
  this.arr = x;
 ;
;

def Q = X(1, [{1 + 2: 1}]);
IO_println(Q.arr);

func(1);
func(1, 2);
func(1, 2, {3:4});
func(); # expected at least 1 arguments but got 0.